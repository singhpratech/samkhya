[package]
name = "samkhya-gpudb"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true
readme = "README.md"
description = "samkhya gpudb engine adapter (CpuFallbackCorrector and GpuCorrector trait)"

[dependencies]
samkhya-core.workspace = true

[features]
default = []
# Real CUDA / Metal kernels land behind these flags; default builds never
# link a GPU runtime. See `src/lib.rs` for the planned wiring.
cuda = []
metal = []
