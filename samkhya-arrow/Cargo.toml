[package]
name = "samkhya-arrow"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true
readme = "README.md"
description = "Arrow integration for samkhya: build portable statistics sketches from Arrow arrays"

[dependencies]
samkhya-core.workspace = true
# Pin to the Arrow major DataFusion 46 vendors (54.3.1 in this workspace's
# resolved graph) so consumers that already pull DataFusion don't end up
# with two parallel Arrow stacks.
arrow = "54"
